package server;

// This interface defines the contract for the HelloWorld service.
// It includes a method that takes a parameter and returns a greeting message.
public interface HelloWorld {
    String getHelloWorld(String param);
}
